/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'facebook.com"', 'twitter.com"', 'instagram.com"', 'images.unsplash.com'],
      },
    };
    
    export default nextConfig;